# --- Web Framework ---
fastapi
uvicorn[standard]
# gunicorn # Optional for some deployment types, but good to have

# --- Database & ORM ---
sqlalchemy[asyncio]
asyncpg
greenlet
alembic # For database migrations

# --- Pydantic & Config ---
pydantic
pydantic-settings
email-validator

# --- Authentication & Security ---
firebase-admin
python-jose[cryptography]
passlib[bcrypt]
cryptography

# --- Trading & Financial Libraries ---
ccxt
#MetaTrader5
mt5linux

# We specify a known good version from PyPI. If it fails, we can use the git method.
#pandas-ta>=0.3.14

# --- ML & AI LIBRARIES ---
# Core ML/Data Science
# CRITICAL: Pin NumPy to prevent conflicts with torch/onnxruntime
numpy<2.0,>=1.23
pandas
scikit-learn
xgboost

# Deep Learning (PyTorch)
# These lines MUST remain here. The Dockerfile's first pip install
# will satisfy them from the CPU index, and the second pip install
# will see they are already satisfied and skip them.
torch==2.1.0
torchvision==0.16.0
torchaudio==2.1.0

# Explainable AI
shap

# ML Lifecycle Management
mlflow-skinny

# --- LLM & Conversational AI ---
langchain
langchain-community
langchain-core
langchain-ollama
sentence-transformers
faiss-cpu

# --- Stream Processing & Async ---
kafka-python
aiokafka
redis
httpx

# --- Protocols & Utilities ---
simplefix
docker
watchfiles # For Celery hot-reloading in local dev

